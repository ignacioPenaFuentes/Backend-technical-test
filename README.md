Instructions for deploy and use:

1. Run the following commands in client:
   mvn clean
   mvn install
2. Run the following commands in server:
   mvn clean
   mvn install
3. Run de Main Springboot Aplication.
